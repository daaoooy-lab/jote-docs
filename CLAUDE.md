# Jote Docs — 작업 규칙

이 저장소는 개인 프로젝트 **Jote**의 기획/설계/개발 과정을 기록하는 문서 저장소다. 새 문서를 작성하거나 정리할 때 아래 규칙을 따른다.

<br/><br/>

## 폴더 구조

- 대분류는 `01-`, `02-`, `03-` ... 순차 번호 접두사로 구분한다.
  - 엄격한 진행 순서를 의미하는 게 아니라 카테고리 구분용이다.
  - 예: `01-planning`, `02-design`, `03-architecture`
  - 새 대분류가 필요하면 다음 번호를 이어서 사용한다. (번호를 비워두거나 10단위로 증가시키지 않는다)

<br/><br/>

## 파일명 규칙

- 파일명 = `YYYY-MM-DD-keyword.md`
- 키워드는 영어(kebab-case)로 작성한다.
- 같은 날짜에도 논의 주제가 다르면 파일을 분리한다. (하루에 여러 파일 생성 가능)

<br/><br/>

## 문서 템플릿

```markdown
# 문서 제목

> ### 문서 개요
> - 작성 시작: YYYY-MM-DD HH:mm
> - 최종 업데이트: YYYY-MM-DD HH:mm
> - 핵심 내용 한줄 요약: ...
> - 관련 문서: [파일명](상대경로) (없으면 생략)

<br/><br/>

## 섹션 제목

내용...

<br/><br/>

## 다음 섹션 제목

### 하위 섹션

내용...

<br/><br/>

## 수정 히스토리

- YYYY-MM-DD HH:mm — 변경 내용 요약
```

- 최상위 제목은 `#`, 이후 섹션은 `##` → `###` → `####` 순으로 사용한다.
- `##` 섹션 간 구분은 `---` 대신 `<br/><br/>`로 통일한다.
- `###` 이하 하위 섹션 간 구분은 `<br/>` 1개로 통일한다.
- 섹션 제목에 이모티콘을 사용하지 않는다. 이모티콘은 본문 내 강조 목적일 때만 예외적으로 허용한다.
- 문서 맨 마지막에 `## 수정 히스토리` 섹션을 두고, 수정할 때마다 항목을 추가한다.
  - 최신 항목이 위로 오도록 최신순으로 쌓는다.
  - 형식: `- YYYY-MM-DD HH:mm — 변경 내용 요약`
  - 문서를 수정할 때는 상단 개요의 "최종 업데이트"도 함께 갱신한다.

<br/><br/>

## 문서화 트리거

- 사용자가 대화 중 `~현재 내용 문서 정리`라고 말하면(맨 앞 `~`는 일반 대화와 구분하기 위한 명령 표시), 그 시점까지의 논의 내용을 위 규칙(폴더 구조, 파일명, 문서 템플릿)에 맞춰 문서로 작성한다.
- 어느 대분류 폴더에 넣을지는 내용을 보고 스스로 판단해서 정한다. 애매하면 사용자에게 확인한다.
- 과정 기록 중심의 저장소이므로, 기존 문서를 수정하기보다 **항상 새 파일을 생성**한다.
- 내용상 이전에 작성된 문서와 연관이 있다면, 새 문서의 "관련 문서" 항목에 해당 문서를 링크로 명시한다.

<br/><br/>

## 커밋 규칙

- 커밋 메시지는 영어로 작성한다. (문서 본문은 한국어 유지)
- 형식:
  ```
  tag: Title

  Body
  ```
- 태그 (필요에 따라 추가):
  - `docs`: 문서 추가/수정
  - `chore`: 폴더 구조, 설정 등 관리성 작업
  - `fix`: 문서 내용 오류 수정
- 본문은 변경 사항을 `-` bullet으로 나열한다.

<br/>

**예시**
```
docs: Add Jote feature brainstorming doc and project conventions

- Add 01-planning/2026-07-26-feature-brainstorming.md
  (brainstorm basic note features, AI features, and extra ideas)
- Define folder structure, file naming, doc template, revision history,
  and documentation trigger rules in CLAUDE.md
```

<br/>

**트리거**
- 사용자가 `~문서 파일 커밋`이라고 말하면, 변경된 문서 파일들을 위 규칙에 맞춰 커밋한다.
- 여러 문서가 함께 변경되었어도 하나로 묶지 않고, 문서 파일 단위로 각각 별도 커밋한다.
